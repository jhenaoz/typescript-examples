// "use strict";  // Enable if the strict mode is false

// this.implements = 123;

// interface Animal {
//   name: string,
//   width: number,
//   height: number
// };

// class Cat implements Animal {
//   name: string = 'Cat';
//   width: number;
//   height: number;

//   public setDimensions(width: number, height: number) : void {
//     this.width = width;
//     this.height = height;
//   }

//   public showNameAndDimensions() : string {
//     return `The ${this.name} has the dimensions ${this.width} x ${this.height}`;
//   }
// }

// const littleCat = new Cat();
// littleCat.setDimensions(.5, .6);
// console.log(littleCat.showNameAndDimensions());

// Date.now = () => 0.5;
